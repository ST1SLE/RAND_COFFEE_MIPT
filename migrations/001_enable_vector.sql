-- Включаем расширение для работы с векторами
CREATE EXTENSION IF NOT EXISTS vector;

-- Добавляем поле "О себе" для генерации эмбеддингов
-- Проверяем, существует ли колонка, чтобы скрипт можно было запускать повторно
ALTER TABLE users 
ADD COLUMN IF NOT EXISTS bio TEXT DEFAULT NULL;

-- Добавляем поле для хранения вектора (размер 384 для paraphrase-multilingual-MiniLM-L12-v2)
ALTER TABLE users 
ADD COLUMN IF NOT EXISTS embedding vector(384);

-- Индекс для ускорения поиска по косинусному расстоянию пока не создаем.
-- Для <10,000 записей полный перебор (seq scan) работает достаточно быстро,
-- а неправильно настроенный индекс может его замедлить. Добавим позже при необходимости.